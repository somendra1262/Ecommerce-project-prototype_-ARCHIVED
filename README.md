# Ecom-Project
A multi-role ecommerce platform consisting of vendors, moderators, an administrator and customers. It uses JWT token based authentication, but sessions for vendor-specific actions since vendors can be removed at any time.
all entities (products,customers,vendors,vendor_requests) are implemented as normalized relational tables
